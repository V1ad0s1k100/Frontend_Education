import { tabsSelectors, tabsData } from './tabs.js'

// Переименование переменной при импорте

// import { selecrors as tabsSelectors3 } from './tabs.js'

const modalsSelectors = { blomblomblom: 12345 }

export default function () {
  console.log(`initModals!`)
}

// Вызов функции делается в файле main.js

// initModals()

// console.log('tabsSelectors:', tabsSelectors)

// console.log('tabsData:', tabsData)

const selecrors = { selecrors: 124 }

// console.log('tabsSelectors3:', tabsSelectors3)
