const slidersSelectors = { blablabla: 123 }

export default function () {
  console.log(`initSliders!`)
}

// Вызов функции делается в файле main.js

// initSliders()

const selecrors = { selecrors: 125 }
